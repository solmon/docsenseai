# paperless-ngx Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-07

## Active Technologies
- Python 3.10+ (supports 3.10-3.14) + Django ~5.2.5, django-allauth ~65.12.1 (with socialaccount and mfa), Django REST Framework ~3.16, drf-spectacular ~0.28 (001-multi-idp-auth)
- Existing Django database (PostgreSQL/MySQL/MariaDB/SQLite) - no new storage requirements (001-multi-idp-auth)
- Python 3.10-3.14 (minimum 3.10, per constitution) + Django 5.2+, Django REST Framework 3.16, Azure Blob Storage client library (azure-storage-blob), Path operations (pathlib) (001-storage-backends)
- PostgreSQL or MariaDB for database metadata, abstract storage layer for documents/archives/thumbnails (filesystem or Azure Blob Storage) (001-storage-backends)
- Python 3.10-3.14 (Django project) + Django, azure-storage-blob (for Azure backend), existing storage backend abstraction (002-tenant-storage-segregation)
- Filesystem (via Django settings) or Azure Blob Storage (via connection string), abstracted through StorageBackend interface (002-tenant-storage-segregation)

- Python 3.10-3.14 (minimum 3.10) - existing + Django 5.2+, Django REST Framework 3.16, Celery 5.5.1, Redis 5.2.1, Angular/TypeScript - existing (001-multi-tenancy)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.10-3.14 (minimum 3.10) - existing: Follow standard conventions

## Recent Changes
- 002-tenant-storage-segregation: Added Python 3.10-3.14 (Django project) + Django, azure-storage-blob (for Azure backend), existing storage backend abstraction
- 001-storage-backends: Added Python 3.10-3.14 (minimum 3.10, per constitution) + Django 5.2+, Django REST Framework 3.16, Azure Blob Storage client library (azure-storage-blob), Path operations (pathlib)
- 001-multi-idp-auth: Added Python 3.10+ (supports 3.10-3.14) + Django ~5.2.5, django-allauth ~65.12.1 (with socialaccount and mfa), Django REST Framework ~3.16, drf-spectacular ~0.28


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
